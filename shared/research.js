// TODO: reference at each level needs to hold the structures that can be built e.g:
// we shouldn't store this in the instance because then we'd have to update all instances as you level up the tech

// currentProductionLevel = 0;
// production.categories = [['xxx','yyy'], ['xxx','yyy','zzz']]
module.exports = {
    production: {
        categories: [
            []
        ]
    }
}